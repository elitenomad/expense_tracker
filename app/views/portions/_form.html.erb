<%= form_for(@portion) do |f| %>
  <% if @portion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portion.errors.count, "error") %> prohibited this portion from being saved:</h2>

      <ul>
      <% @portion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :expenses_id %><br>
    <%= f.number_field :expenses_id %>
  </div>
  <div class="field">
    <%= f.label :payee_id %><br>
    <%= f.number_field :payee_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
