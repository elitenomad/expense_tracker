<h1>
  
  <%= @group.name %>
</h1>

<div class="row">
  <div class="col-md-6">
 
  </div>
  <div class="col-md-6">
    <%= link_to "Manage Group Members", groupuser_path(@group), class:'btn btn-default' %>
    <%= link_to "Add Expense", new_group_expense_path(@group), class:'btn btn-default' %>
  </div>
</div>

<%= link_to "Settle", "/groups/#{@group.id}/settlements", method: :post %>

<div class="row">
  <h3>Overview</h3>
  <div class="col-md-6">
    <table class="table table-bordered table-striped">
          <tr>

            <th>Group Members</th>
            <th>Invested </th>
            <th>Portion </th>
            <th>Balance </th>
          </tr>
          <% @user_portions_hash.each do |user,amount| %>
          <tr>
            <td><%=  user.name %></td>
            <td><%=  @user_invested_hash[user.name] %></td>
            <td><%=  @user_portion_hash[user.name] %></td>
            <td><%=  number_to_human(content(user,amount), precision:2) %></td>
          </tr>
        <% end %>
    </table>
  </div>
</div>

<%= render "expenses/expense" %>



