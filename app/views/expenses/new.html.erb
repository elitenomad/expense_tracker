<%= content_for(:title, "Add Expense for #{@group.name}") %>

<div class="container">
  <div class="row">
    <div class="col-md-4">
      <%= render 'form' %>  
      <%= link_to 'Back', group_path(@group) %>
    </div>
  </div>  
</div>

